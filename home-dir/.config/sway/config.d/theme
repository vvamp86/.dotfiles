# Apply gtk theming
exec_always ~/.config/sway/scripts/import-gnome-settings

# scale settings
output eDP-1 scale 1

# Thin borders:
# Uncomment to get rid of borders when there is only a single application
#smart_borders on
#default_border normal

#border indicator on windows:
default_border pixel 1
default_floating_border pixel 2

# Set inner/outer gaps
gaps inner 2
gaps outer 1

# Hide titlebar on windows:
default_border pixel 1

# Default Font
font pango: FiraCode Nerd Font 10

# Set wallpaper:
exec_always swaybg -i "/usr/share/backgrounds/wallpaper.png" -m fill

# Title format for windows
for_window [shell="xdg_shell"] title_format "%title (%app_id)"
for_window [shell="x_wayland"] title_format "%class - %title"

#################
# Window Colors #
#################
# Define colors variables:
set $darkblue       #0d3138
set $lightblue      #80aa9e
set $urgentred      #fb4934
set $white          #e2cca9
set $black          #141617
set $purple         #d3869b
set $darkgrey       #282828
set $grey           #3c3836
set $mediumgrey     #504945
set $yellowbrown    #e9b143
set $orange         #8a5328

# define colors for windows:
#class                      border      bground     text        indicator       child_border
client.focused              $lightblue  $darkblue   $white      $orange         $orange
client.focused_inactive     $darkblue   $darkblue   $grey       $black          $black
client.unfocused            $darkblue   $darkblue   $grey       $darkgrey       $darkgrey
client.urgent               $urgentred  $urgentred  $white      $yellowbrown    $yellowbrown
client.placeholder          $darkblue   $darkblue   $grey       $orange         $orange
client.background           $white

##############
# Status Bar #
##############
# Read `man 5 sway-bar` for more information about this section.
bar {
    swaybar_command waybar
}
